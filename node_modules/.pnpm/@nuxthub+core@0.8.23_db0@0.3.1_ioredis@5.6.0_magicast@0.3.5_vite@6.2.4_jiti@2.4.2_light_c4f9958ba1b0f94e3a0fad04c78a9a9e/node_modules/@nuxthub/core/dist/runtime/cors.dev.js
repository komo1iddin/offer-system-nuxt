import { handleCors, eventHandler } from "h3";
export default eventHandler((event) => {
  handleCors(event, {
    methods: "*",
    origin: [
      "https://admin.hub.nuxt.com",
      "https://hub.nuxt.dev",
      /http:\/\/localhost:\d+/
    ]
  });
});
